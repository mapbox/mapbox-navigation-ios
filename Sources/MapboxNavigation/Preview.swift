import MapboxMaps

// :nodoc:
public class Preview {
    
}

extension Preview {
    
    // :nodoc:
    public enum CameraMode {
        
        case idle
        case centered
        case following
    }
}
